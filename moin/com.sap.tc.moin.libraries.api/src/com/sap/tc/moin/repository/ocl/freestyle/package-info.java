/**
 * The so-called "freestyle" OCL API allow clients to define OCL expressions
 * that can be used to query model data at runtime. A special use case of such
 * queries could be the definition of additional constraints (i.e. queries that
 * result in a Boolean value), but OCL is not limited to constraints, but can be
 * used to specify arbitrary queries. <br>
 * <br>
 * This API is for SAP-internal use only and subject to change
 */
package com.sap.tc.moin.repository.ocl.freestyle;