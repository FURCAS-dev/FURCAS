package de.hpi.sam.bp2009.solution.eventManager.framework;

/**
 * The <code>RegistrationHandle</code> originally was returned when clients registered as listener on the
 * EventRegistry. This handle was only needed when clients wanted to undo their registration call. This feature was
 * temporarily removed due to missing "real world use cases", but it might be reintroduced in future.
 * 
 * @author Daniel Vocke (D044825)
 */
public interface RegistrationHandle {

}